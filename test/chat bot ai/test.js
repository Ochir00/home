let todoData = ['Buy groceries', 'Clean the house', 'Write code', 'Read a book'];
let index = 2;

// Remove the item at index 2 (the third element, 'Write code')
todoData.splice(index, 1);

console.log(todoData);  // ['Buy groceries', 'Clean the house', 'Read a book']
